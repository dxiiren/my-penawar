# Project memory — my-penawar

Index of durable, single-fact memories for this repo. One line per memory:
`- [Title](file.md) — hook (when to read it)`. Each fact lives in its own `*.md`
file next to this index, with `name` / `description` / `metadata.type`
(`reference` | `feedback` | `project`) frontmatter.

## Memories

_No memories yet._
